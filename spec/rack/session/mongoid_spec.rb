require 'spec_helper'

describe Rack::Session::Mongoid do
  it 'has a version number' do
    expect(Rack::Session::Mongoid::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
